<?php

/**
 * Created by PhpStorm.
 * User: user
 * Date: 19.11.2017
 * Time: 18:30
 */
class CallbackController extends BaseController
{
    private $secret = 'qwAFwe12Qwefh';
    /** @var $admin BotEntity  */
    private $bot   = null;

    public function init()
    {
        $this->bot = new BotEntity();
        parent::init(); // TODO: Change the autogenerated stub
    }

    /**
     * @return null
     */
    public function actionVk()
    {
        if (!isset($_REQUEST)) {
            echo 'ok';
            exit();
        }

        $data = json_decode(file_get_contents('php://input'));
        if ($data->type == 'board_post_new' && $data->topic_id == Globals::$config->topic_id) {
            VkSdk::addComment($this->bot->getToken(), $data->from_id);
        }

        echo 'ok';
        exit();
    }
}